# Fixtures

Sample `.ograf.json` packages used to cross-test our export pipeline against the reference `ograf-devtool`, and as unit-test inputs for `packages/validation`.

Populated starting Phase 4.
