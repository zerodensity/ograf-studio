# Handover — 2026-09-03 — OGraf Studio 0.11

## Release scope

The user authorized publishing the latest work, bumping the version and preparing single-file
executables again. The public release target is `zerodensity/ograf-studio`, matching the existing
0.10 release and repository branding. The older `savasalkim` remote remains configured separately.

0.11 includes native path paint/masks, procedural tiling, composable effects, the dockable Brand Kit,
live color/effect bindings, smaller interface typography and the matching AI authoring guidance.
Personal Graphite project files remain in Documents and are not added to the source repository.

## Version and source identity

- Product/tag: 0.11 / `v0.11`.
- Root and MCP package versions: 0.11.0. Windows file version: 0.11.0.0.
- Scene document version remains 28; no schema migration is introduced by the release bump.
- Build from an isolated checkout of the release commit, with fresh lockfile dependencies.
- `RELEASE-MANIFEST.json` and `SHA256SUMS.txt` on the GitHub release record exact source and artifact
  identities; binaries are release assets, not source commits.

## Verification and publication procedure

- Run `npm run verify`, then the standalone matrix and structural artifact checks.
- Smoke-test Windows startup, MCP and browser/export behavior, plus Linux x64 under Ubuntu WSL.
- Native macOS/ARM64 checks and signing are unavailable; keep that boundary explicit in release notes.
- Fast-forward public `main`, verify CI, then publish a new tag/release with matching uploaded hashes.
- Do not move historical tags. An existing v0.08 tag differs between the two remotes; release work
  fetches public main without tags and creates only the new v0.11 tag.

## Follow-up

Shared pattern lighting remains queued. Preserve the saved Graphite Lower Third template and the
current Studio session. No headless conformance work or cross-project component-library work is
included in this release.
