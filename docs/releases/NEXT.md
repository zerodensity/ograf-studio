# OGraf Studio — Next release

## Cross-platform standalone distribution

- Added repeatable macOS Intel/Apple Silicon and Linux x64/ARM64 single-executable targets beside the
  Windows build, with format/architecture/content/hash verification and a real Linux x64 WSL smoke
  test. The new binaries are attached to the existing 0.10 release; native macOS and ARM64 runtime
  validation plus platform signing remain pending.

This file is the unreleased changelog. Add completed, verified changes here before assigning the
next version and tag.
